// REGRAS SIMPLIFICADAS PARA TESTE - FIRESTORE
// Use estas regras TEMPORARIAMENTE para testar se o problema é nas regras

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGRAS ABERTAS PARA TESTE - REMOVER EM PRODUÇÃO
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

// INSTRUÇÕES:
// 1. Copie estas regras no console do Firebase
// 2. Publique
// 3. Teste o calendário
// 4. Se funcionar, o problema era nas regras complexas
// 5. Depois volte para as regras de produção
