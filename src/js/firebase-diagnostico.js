// Diagnostic tool removed. No-op placeholder to avoid runtime errors.
// Portuguese-named stub
window.diagnosticoFirebase = function() {
    console.log('diagnosticoFirebase() called but diagnostic tool was removed.');
    return Promise.resolve();
};
